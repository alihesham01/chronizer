# Database — PostgreSQL
DATABASE_URL=postgres://woke_user:CHANGE_ME_STRONG_PASSWORD@localhost:5432/woke_portal
DB_POOL_SIZE=50

# JWT — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=CHANGE_ME_TO_RANDOM_64_HEX
JWT_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=production

# CORS — set to your frontend domain in production
CORS_ORIGIN=https://yourdomain.com

# Registration invite code — generate with: node -e "console.log('WP-' + require('crypto').randomBytes(16).toString('hex').toUpperCase())"
INVITE_CODE=CHANGE_ME_TO_SECURE_CODE

# Optional: PgBouncer (recommended for production)
# DATABASE_URL=postgres://woke_user:password@localhost:6432/woke_portal
